# Cache policy for static assets (CSS)
# Goal: avoid "stale CSS" after uploads without needing manual ?v=123 bumps.
#
# Notes:
# - "no-cache" still allows caching, but forces the browser to revalidate with the server.
# - This keeps desktop/mobile UI unchanged; it's only HTTP headers for assets.

<IfModule mod_headers.c>
  <FilesMatch "\.(css|map)$">
    Header set Cache-Control "no-cache, max-age=0, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>
</IfModule>


